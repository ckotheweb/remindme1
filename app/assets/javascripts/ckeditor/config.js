CKEDITOR.editorConfig = function( config ) {
    // Define changes to default configuration here. For example:
    // config.language = 'fr';
    // config.uiColor = '#AADC6E';
     config.height = '30vh';
     config.width = '70vh';
     
config.removePlugins = 'elementspath,save,preview,print,newpage,source,pastefromword';
}
